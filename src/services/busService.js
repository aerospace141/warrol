// Service layer for API calls - will be implemented later
export const busService = {
  // Search buses
  searchBuses: async (searchParams) => {
    // API call implementation will go here
    return Promise.resolve([]);
  },

  // Get bus details
  getBusDetails: async (busId) => {
    // API call implementation will go here
    return Promise.resolve({});
  },

  // Book seats
  bookSeats: async (bookingData) => {
    // API call implementation will go here
    return Promise.resolve({});
  }
};